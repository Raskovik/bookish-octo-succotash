// Re-exported from the master config (src/config.ts) — edit the flag
// there. Kept here too so every existing `from "@/lib/feature-flags"`
// import keeps working.
export { TRADING_ENABLED } from "@/config";
